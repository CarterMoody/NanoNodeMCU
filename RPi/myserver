/usr/bin/python3.7 /home/pi/Desktop/NanoNodeMCU/RPi/YoutubeMqttNano.py

# Capture the return code of the above line of code (launching python)
#	If it's not zero, go ahead and exit THIS script with an error code.
#	This will 'forward' the error code over to the ./forever script.
ret=$?
if [ $ret -ne 0 ]; then
    #Handle failure
    #exit if required
    exit 69
fi
